import React from 'react';

const Home=()=>{
    return(
        <div className={'container jumbotron'} style={{marginTop:20}}>
            <h1 className="lead">Welcome to Media Library built with React, Redux, and Redux-saga </h1>
        </div>
    )
};
export default Home;